<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="estate_property_form_view" model="ir.ui.view">
        <field name="name">estate.property.form.view</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Test">
                <header>
                    <button name="property_sold" type="object" string="SOLD" style="background-color: #71639e; color: white;"/>
                    <button name="property_cancel" type="object" string="CANCEL" style="background-color: #71639e; color: white;"/>
                </header>
                <sheet>
                    <h1>My new house</h1>
                    <field name="name"/> <!-- add -->
                    <!-- <field name="tag_ids" widget="many2many_tags"/> -->
                    <group>
                        <group>
                            <field name="state" string='Status'/>
                            <field name="property_type_id" string="Property Type"/>
                            <field name="postcode" string="Postcode"/>
                            <field name="date_availability" string="Available From"/>
                        </group> 
                        <group>
                            <field name="expected_price" string="Expected Price"/>
                            <field name="best_price" string="Best Offer"/>
                            <field name="selling_price" string="Selling Price"/> 
                        </group>
                    </group>               
                    

                    <sheet>
                        <notebook>
                            <!-- Pestaña 1 -->
                            <page string="Description">
                                <group>
                                    <field name="description"/>
                                    <field name="bedrooms"/>
                                    <field name="living_area" string="Living Area (sqm)"/>
                                    <field name="facades"/>
                                    <field name="garage"/>
                                    <field name="garden"/>
                                    <field name="garden_area" string="Garden Area (sqm)"/>
                                    <field name="garden_orientation"/>
                                    <field name="total_area"/>
                                    <field name="active" string="Active"/> <!-- add -->
                                </group>
                            </page>

                            <!-- Pestaña 2 -->
                            <page string="Offers">
                                <!-- <group> -->
                                <field name="offer_ids" string="Property Offer"/>
                                <!-- </group> -->
                            </page>
                            
                            <!-- Pestaña 3 -->
                            <page string="Other Info">
                                <group>
                                    <field name="seller" string="Salesman"/>
                                    <field name="buyer" string="Buyer"/>
                                </group>
                            </page>
                            
                        </notebook>
                    </sheet>
                    <!-- <div>
                        <button string="Guardar" type="object" name="action_guardar" class="btn-primary"/>
                    </div> -->
                </sheet>
            </form>
        </field>
    </record>

</odoo>