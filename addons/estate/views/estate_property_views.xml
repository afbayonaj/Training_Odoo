<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Menu -->
    <!-- 
      id: Se puede utilizar para hacer referencia al registro (sin conocer 
      su identificador en la base de datos).

      model: tiene un valor fijo de ir.actions.act_window.

      name: Es el nombre de la accion.

      res_model: Es el model al que se aplica la accion.

      view_mode: son las vistas que estarán disponibles; en este caso son 
      las vistas de lista (árbol) y formulario. Veremos más adelante que 
      puede haber otros modos de visualización. 
    -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_state': True}</field>
        <!--
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Define a new lost reason
          </p><p>
            Use lost reasons to explain why an opportunity is lost.
          </p><p>
            Some examples of lost reasons: "We don't have people/skill", "Price too high"
          </p>
        </field>
        -->
    </record>
</odoo>